{
  "name": "acs-worker",
  "version": "1.0.0",
  "description": "Agent Credit Score API — Cloudflare Worker",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.7.0"
  }
}
